# Database - Prisma uses both URLs with Neon:
# DATABASE_URL: pooled (for app queries) - host contains -pooler
# DIRECT_URL: direct (for migrations) - host without -pooler
# Get both from Neon dashboard: Connect -> toggle "Pooled"
DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require
DIRECT_URL=postgresql://user:pass@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# For local PostgreSQL, use same URL for both:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/sneaker_drop
# DIRECT_URL=postgresql://postgres:password@localhost:5432/sneaker_drop

# Server
PORT=3001

# CORS - Frontend URL for API and Socket.io
CORS_ORIGIN=http://localhost:5173

# Client (Vite) - set these when building for production (e.g. Vercel)
# Leave empty for local dev (uses Vite proxy)
VITE_API_URL=http://localhost:3001
VITE_SOCKET_URL=http://localhost:3001
# Production example: VITE_API_URL=https://sneaker-drop-api.onrender.com

VITE_API_URL=https://sneaker-drop-system.onrender.com/
VITE_SOCKET_URL=https://sneaker-drop-system.onrender.com/

